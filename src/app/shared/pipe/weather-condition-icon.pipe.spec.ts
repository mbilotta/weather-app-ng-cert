import { WeatherConditionIconPipe } from './weather-condition-icon.pipe';

describe('WeatherConditionIconPipe', () => {
  it('create an instance', () => {
    const pipe = new WeatherConditionIconPipe();
    expect(pipe).toBeTruthy();
  });
});
